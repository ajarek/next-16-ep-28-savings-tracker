    export type Record = {
        id: number,
        name: string,
        targetAmount: number,
        currentAmount: number,
        icon: React.ReactNode,
        deadline: string,
        priority: string,
    }