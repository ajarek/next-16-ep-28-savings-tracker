    export type Record = {
        id: number|string,
        name: string,
        targetAmount: number,
        currentAmount: number,
        icon: string,
        deadline: string,
        priority: string,
    }